<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>獎學金列表</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <h1>獎學金列表</h1>
        <% data.forEach(student => { %>
            <div>
                <ul>
                    <li class="students-list"><a href="/students/<%= student.id %>" ><%= student.name %></a> </li>
                    <li class="students-list"><a href="/students/edit/<%= student.id %>" >更新</a> </li>
                    <li class="students-list"><a href="/students/delete/<%= student.id %>" >刪除</a> </li>
                </ul>
            </div>
        <%}) %> 
    <a href="/students/insert">返回獎學金首頁</a>
</body>
</html>